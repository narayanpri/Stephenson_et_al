// For analysis yielding processed transcriptomic data in Table S2:

The processing commands executed to go from raw sequence files to enumerate counts is described in 
APOE3_DGAT_LPS_nf_core_commands.txt

The processing commands executed in R to go from counts to differential results is described in 
APOE3_DGAT_LPS_Rcode.txt


// For analysis yielding processed transcriptomic data in Table S1 and S4:

The processing commands executed to go from raw sequence files to enumerate counts is described in 
APOE4_DGAT_LPS_nf_core_commands.txt

The processing commands executed in R to go from counts to differential results is described in 
APOE4_DGAT_LPS_Rcode.txt
